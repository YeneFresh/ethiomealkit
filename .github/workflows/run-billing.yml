name: "Daily Billing Cron (Secure)"
on:
  schedule:
    - cron: '0 6 * * *'
  workflow_dispatch:

jobs:
  billing:
    name: Run daily billing
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
      - name: Run billing script
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          echo "Running billing at $(date -u)"
          # TODO: replace with real billing commands

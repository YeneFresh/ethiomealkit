import 'package:flutter/material.dart';

/// YeneFresh brand color palette
class BrandColors {
  // Primary brown-gold theme
  static const primary = Color(0xFFC6903B); // gold/brown
  static const primaryDark = Color(0xFF8B5E2B);
  static const onPrimary = Colors.white;

  // Optional surface overlays
  static const overlayWarm = Color(0xFFC6903B);

  // Extended palette (for consistency with AppColors)
  static const gold = Color(0xFFC8A15A);
  static const darkBrown = Color(0xFF3E2723);
  static const offWhite = Color(0xFFFAF8F5);
}
